{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Batch script build",
            "type": "shell",
            "command": "${workspaceFolder}/build.bat",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
